<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.omni.code.mapper.ProductMapper">

    <select id="searchProducts" resultType="Product">
        SELECT product_id AS productId, picture_base64 AS pictureBase64, name, description, price, features,
        average_rating AS averageRating, created_at AS createdAt
        FROM product
        WHERE 1=1
        <if test="name != null">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="rating != null">
            AND average_rating &gt;= #{rating}
        </if>
        <if test="minPrice != null">
            AND price  &gt;= #{minPrice}
        </if>
        <if test="maxPrice != null">
            AND price &lt;= #{maxPrice}
        </if>
        LIMIT #{offset}, #{size}
    </select>

    <select id="getTotalCount" resultType="long">
        SELECT COUNT(*) FROM product
        WHERE
        <if test="name != null">
            name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="rating != null">
            AND average_rating &gt;= #{rating}
        </if>
        <if test="minPrice != null">
            AND price  &gt;= #{minPrice}
        </if>
        <if test="maxPrice != null">
            AND price &lt;= #{maxPrice}
        </if>
    </select>

</mapper>
